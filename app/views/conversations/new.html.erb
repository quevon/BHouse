<section class="main">
    <h1>New Conversation</h1>

    <%= form_tag conversations_path, method: :post do %>

        <%= hidden_field_tag :owner_id, @recipient %>

        <div>
            <%= text_area_tag :body %>
        </div>

        <%= submit_tag %>
    <% end %>


</section>